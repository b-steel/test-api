setup:
  config:
    DJANGO_DEBUG: True
    DJANGO_SECRET_KEY: "w@9pt7o7an7=lio8!=2o_=0ez7%a@5^fzq11nn75fgk0*k&&d9"
    DJANGO_ALLOWED_HOSTS: .herokuapp.com 127.0.0.1 localhost [::1]
build:
  docker:
    web: Dockerfile
run:
  web: gunicorn --bind 0.0.0.0:$PORT test_api.wsgi:application
