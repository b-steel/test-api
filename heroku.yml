setup:
  config:
    DJANGO_DEBUG: True
    DJANGO_SECRET_KEY: "&7+od6r_4k8&x_uxv7*%)sqd28!c2+!a_wre!pqw)y(^gr)e$_"
    DJANGO_ALLOWED_HOSTS: .herokuapp.com 127.0.0.1 localhost [::1]
build:
  docker:
    web: Dockerfile
run:
  web: gunicorn --bind 0.0.0.0:$PORT test_api.wsgi:application
